<% content_for(:title) { "New User — Factor Fifty" } %>

<div class="page-header">
  <div>
    <h1 class="page-title">New User</h1>
  </div>
</div>

<%= render "form", keycloak_user: @keycloak_user, assignable_roles: @assignable_roles,
                    resellers: @resellers, customers: @customers, mode: :new %>
